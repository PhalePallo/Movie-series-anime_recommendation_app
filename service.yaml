apiVersion: v1
kind: Service
metadata:
  name: msa-app-service
spec:
  type: NodePort
  selector:
    app: msa-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
